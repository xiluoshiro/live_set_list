# 专辑页面链接与多封面实现方案

状态：已实现（2026-09-27），本地验证记录见文末。本文细化[歌曲资料与专辑需求](../product/song-details-and-albums.md)中的专辑链接、多封面和默认封面规则。应用已改用 `album_url` / `cover_urls`，旧 `cover_path` 仅暂留数据库。

## 1. 范围与业务规则

首期由管理员手动维护专辑页面 URL 和封面 URL 数组。图片由浏览器直接从外部地址加载，歌曲通过收录关系复用专辑封面。

| 字段 | API 类型 | 数据库类型 | 规则 |
| --- | --- | --- | --- |
| `album_url` | `string \| null` | `text NULL` | 专辑公告或平台详情页；尚未补录时允许为空，输入空白归一为 `null` |
| `cover_urls` | `string[]` | `text[] NOT NULL DEFAULT '{}'::text[]` | 有序图片 URL 数组；没有封面时为 `[]`，第一项为默认封面 |

- 新增、移除、调整顺序和设为默认都修改同一个数组；不增加默认封面 ID、默认下标或图片资源表。
- “设为默认”把该 URL 移到第一位，其余 URL 的相对顺序保持不变。
- 删除第一项后，新第一项自然成为默认；删除全部图片后保存空数组。
- 访客切换封面只改变当前页面状态，不写数据库，不改变默认值。
- 同一专辑的普通盘、限定盘等图片可以放在同一数组；首期不增加封面名称、版本对象或封面与专辑子项的映射。
- 歌曲和歌曲组不另存封面字段；不同收录专辑各自保有图片数组。

本期不包含页面解析、Spotify/网易云接口接入、图片上传、文件代理、图床、定时抓取或批量补录现有专辑链接。

## 2. 现有入口与改动范围

| 层 | 入口 | 实现 |
| --- | --- | --- |
| 数据库 | [V37 专辑表](../../backend/db/flyway/sql/V37__add_song_catalog_and_albums.sql) | 新增迁移增加两个字段；已执行的迁移保持原样 |
| 请求与响应 | [song_catalog.py](../../backend/app/schemas/song_catalog.py) | `AlbumWrite`、`AlbumUpdate`、`AlbumSummary` 改用两个新字段，详情和歌曲嵌套专辑沿用同一契约 |
| 公共读取 | [song_catalog.py](../../backend/app/song_catalog.py)、[songs.py](../../backend/app/routers/songs.py) | 专辑详情、歌曲详情及组内版本读取返回完整有序数组 |
| Console 读写 | [console_song_catalog.py](../../backend/app/routers/console_song_catalog.py) | 专辑列表、读取、创建、更新、仅更新曲目均保留两字段 |
| 前端 API | [api.ts](../../frontend/src/api.ts) | 更新专辑类型和草稿；继续使用现有资料保存及缓存失效入口 |
| 后台表单 | [AlbumAdminSection.tsx](../../frontend/src/components/console/AlbumAdminSection.tsx) | 页面链接输入、有序封面列表、预览和默认设置 |
| 前台展示 | [SongCatalog.tsx](../../frontend/src/components/SongCatalog.tsx)、[song-catalog.css](../../frontend/src/components/song-catalog.css) | 卡片默认图、展开详情内多图切换、专辑页面链接 |
| 说明文档 | [歌曲实现设计](song-catalog-and-albums.md)、[API 补充规则](../api.md) | 区分目标契约与已实现状态，清理随包封面作为目标方案的说明 |

## 3. 数据库迁移与旧依赖

### 3.1 增加字段

已新增 [V41__add_album_links_and_cover_urls.sql](../../backend/db/flyway/sql/V41__add_album_links_and_cover_urls.sql)：

1. 使用既有 `live_project_owner` 所有权约定增加 `album_url`、`cover_urls`。
2. 旧专辑初始化为 `album_url = NULL`、`cover_urls = '{}'::text[]`；链接由后台后续补录。
3. 约束数组为空或标准一维数组，非空数组下标从 1 开始，元素不得为 NULL；长度上限与 API 一致。
4. 不添加图片表、索引或新运行角色。沿用专辑表现有读取与管理写入授权，普通用户写账号不获得资料维护权限。
5. 迁移前检查旧 `cover_path` 非空值。存在有效旧值时先停止迁移，整理转为外链的映射；不得直接清空或静默丢弃。目标环境必须自行检查，不能依赖其他环境的计数。

SQL 只保证存储结构；URL 格式、逐项长度、空白和重复项由请求模型验证。未来人工 SQL 回填需执行同等数据校验。

### 3.2 应用切换与旧列清理

[现有发布流程](../production-deployment-runbook.md)先迁移数据库，再切换应用版本，并且应用回滚不回滚 schema。因此本次增加字段的迁移暂留旧 `cover_path`，让切换前的代码仍能读取原表。

新应用只读写两个新字段，移除旧路径请求校验、发布目录存在性检查和旧前端输入框；不建立双写、回退取图或长期兼容分支。旧列只是应用切换期间的数据库兼容措施。

旧列的删除另用后续 Flyway 迁移，条件是依赖旧列的应用版本已退出回滚范围、历史回填的使用顺序已明确且旧值复核完成。不要把增加字段和删除旧列同时放入同一批待执行迁移。

[已交付的 V40 回填包](../../backfill/song-catalog-2026-09-26/README.md)的 SQL 和生成模板显式引用 `cover_path`。保持该交付包及其验证哈希；后续文档须注明其适用 schema 阶段。若需要在旧列删除后的数据库重新执行回填，应另行生成匹配新结构的回填文件，不改写已执行交付件，也不为历史脚本永久保留旧列。

本地实现验收使用隔离测试数据库。生产库迁移和远程部署不是本功能完成本地验证的前提。

## 4. 请求校验、事务与 API

### 4.1 URL 与数组校验

- 两类 URL 都只接受有效的绝对 HTTPS 地址，拒绝用户名密码、控制字符、协议相对地址、本机文件路径、`data:` 和 `javascript:` 等协议。
- 每个 URL 最长 2048 字符；数组最多 20 项。去除首尾空白后进行验证，不重写路径、查询参数或来源页面的 fragment。
- `album_url` 的空白输入转换为 `null`；封面数组内的空字符串、NULL、重复 URL 返回 422，错误能定位到对应项。
- 数组省略与空数组按写入语义处理，显式 `cover_urls: null` 返回 422。
- 不以 `.jpg` 等扩展名作为必要条件，支持没有文件扩展名的 CDN 地址。
- 服务端只校验 URL 数据，不访问地址、不执行 HEAD 检测，也不根据远端是否暂时可用阻止保存。前台预览失败不等于 URL 格式非法。
- 手工外链不限制为某几个图片供应商；本期没有服务端抓取入口，因此不引入平台解析器或抓取域名配置。

### 4.2 复用现有接口

| 接口 | 行为 |
| --- | --- |
| `POST /api/console/albums` | 接收两字段；省略时分别使用 `null` 和 `[]` |
| `PUT /api/console/albums/{id}` | 继续要求 `expected_revision`；未提供的新字段保留原值，显式 `null` 或 `[]` 分别清空页面链接或封面数组 |
| `PUT /api/console/albums/{id}/tracks` | 仅修改曲目，保留页面链接和封面数组，包括默认顺序 |
| `GET /api/console/albums`、`GET /api/console/albums/{id}` | 返回两字段，便于管理草稿完整回填 |
| `GET /api/albums/{id}` | 返回相同字段及完整曲目 |
| `GET /api/songs/{id}`、`GET /api/song-groups/{id}` | 嵌套的专辑摘要返回两字段，保持已有按专辑去重的语义 |

示意资料片段：

```json
{
  "album_url": "https://bang-dream.com/discographies/3302/",
  "cover_urls": [
    "https://images.example.test/album-standard.jpg",
    "https://images.example.test/album-limited.jpg"
  ]
}
```

管理表单每次保存明确提交两个字段。后端区分更新时的“未提供”和“显式清空”，并在取得专辑修订锁后确定最终值，避免元数据更新误清空图片。

所有写入继续沿用 editor 权限、session、CSRF、专辑 revision 和审计事务。过期 revision 返回 409；失败时原链接、数组、曲目和 revision 保持不变，前端保留草稿。

API 不暴露 PostgreSQL 的数组下标；JSON 第 0 项和数据库第 1 项都表示同一默认封面。不得通过排序、集合转换或按 URL 字典序重排数组。

成功保存继续调用现有 `songCatalogWrite`，复用歌曲资料缓存清理及跨标签页变更事件。图片切换不调用保存接口，也不触发资料缓存失效。

## 5. 后台管理交互

沿用专辑管理现有的表单、表格、按钮、保存确认和放弃草稿流程：

1. 在资料字段中加入“专辑页面”输入框，替换原“封面文件”单值输入。
2. “封面”区域使用现有 Console 表格样式，每行展示小图预览、URL 输入和操作。
3. 提供“添加封面”“上移”“下移”“设为默认”“移除”。首项的默认操作显示为不可操作的“默认”，避免额外说明段落。
4. 新增行在草稿中可以暂时为空，但保存前必须补全或移除；不静默过滤空行或重复项。
5. 设为默认、排序和移除仅修改草稿，统一走原有保存确认。默认封面不另发单独的写请求。
6. 确认表继续复用 `UpdateDiffTable`，分别展示“专辑页面”“封面”前后值；封面值按顺序编号，并标明首项为默认，使仅调整默认值的修改也清楚可见。
7. 保存中和确认中沿用既有锁定行为；恢复原值、切换专辑、新建专辑、取消确认和 409 错误均保留原有草稿规则。

管理 URL 使用 textarea 或可收缩的输入单元格承载长地址，复用现有输入样式。新增的布局规则仅作用于封面区域，不改共享表格的列宽或全局输入样式。

## 6. 公共页面展示与切换

### 6.1 收录专辑卡片

- 卡片显示 `cover_urls[0]`，保留原有“点击专辑展开详情”的交互。
- 空数组时沿用文字展示，不增加空封面提示。
- 图片按完整内容等比显示，不裁切封面。加载失败不会隐藏专辑标题、发行信息或详情入口。

### 6.2 展开专辑详情

- 在既有展开区域显示封面，并在存在 `album_url` 时提供“专辑页面”外链；新标签页打开并使用 `noopener noreferrer`。
- 首次展开显示第一张。多图时用既有简洁按钮形式提供“上一张”“下一张”和当前位置，例如 `1 / 3`；首尾禁用对应方向，不自动轮播。
- 只有一张时不显示切换控件；空数组时省略图片区域。
- 切换封面不触发收起、跳转歌曲或重新请求专辑详情。控件放在现有专辑按钮外，避免按钮内嵌按钮。
- 切换专辑、切换歌曲版本或重新展开时回到默认图；新资料载入后若 URL 数组发生变化，也从第一张开始。
- 图片切换状态独立于歌曲演奏记录、分页和异步详情请求；继续保留现有请求代次校验，旧请求不能覆盖新选择。
- 加载失败时显示简短失败状态，仍允许切换其他封面或访问专辑页面；不自动修改默认顺序、不跳过失效 URL 写回数据库。
- 使用固定占位尺寸或比例、懒加载及描述图片内容的替代文本；窄屏下图片和切换按钮不得撑宽详情区域。

### 6.3 样式复用与视觉确认

后台封面行与现有专辑曲目排序表在角色、操作、状态和视觉结构上高度相似，直接复用 `console-admin-table`、`console-table-wrap` 及已有按钮样式。保存确认继续复用 `UpdateDiffTable`。

前台继续使用 `song-catalog` 的详情容器、分页按钮和主题变量；只补封面展示所需的局部布局。实现前在浏览器中确认现有页面，完成后检查宽屏、390px 窄屏和深浅色主题。验证结果见文末。

## 7. 实施顺序

1. **契约与迁移**：实现两个字段、存储约束和旧封面前置检查；在隔离库验证从 V40 增量升级及从迁移链重建。
2. **后端读写**：完成 schema、所有专辑读取路径、保存逻辑和仅更新曲目路径；补字段保留、顺序、权限和并发测试。
3. **后台管理**：完成手工链接编辑、预览、排序、默认设置、确认差异和草稿保护。
4. **公共展示**：完成卡片默认图、详情切换、来源链接、加载失败和选择状态重置。
5. **验证与文档**：执行所需自动检查和本地浏览器验证，同步 API 补充说明、迁移说明和功能状态。

旧列删除属于应用切换后的单独收口步骤，不和本次增量迁移一起执行。

## 8. 验证范围与完成标准

### 8.1 后端与真实数据库

- 新专辑省略字段得到 `null` / `[]`；显式单图、多图数组按提交顺序完整往返。
- 调整第一项改变默认封面；清空、移除首项、数组上限和 URL 上限按契约处理。
- 拒绝非 HTTPS、无效 URL、空元素、NULL 元素和去空白后的重复项；允许不带扩展名的有效 CDN URL。
- 只改其他资料且未提供新字段时保留链接；仅修改曲目后页面链接、封面内容和顺序不变。
- 过期 revision、越权或 CSRF 失败不产生部分更新；普通用户账号不能写专辑资料。
- 通过管理详情、公共专辑详情、歌曲详情和歌曲组详情读取一致数据；同一专辑的多次收录不会产生重复封面条目或改变演奏次数。
- V40 增量迁移保留已有专辑 ID、曲目关系、revision、旧列读取能力和对象所有权；旧字段含非空数据时明确停止。
- 数据库数组约束通过真实 SQL 验证，不用预设 mock 结果代替存储顺序和约束检查。

### 8.2 前端行为

- 添加、排序、设为默认、移除、恢复、取消及保存失败均验证用户可见结果和实际提交数组。
- 仅调整默认图也会产生待保存状态，并出现在确认差异中。
- 卡片使用首图；展开详情默认首图；上一张/下一张正确切换，单图不出现无意义的切换控件。
- 图片失败、切换专辑、切换版本和数组更新后仍可操作，选择状态不会串到另一张专辑。
- 点击图片切换控件不触发专辑收起或歌曲跳转，且不会发送写请求。
- 资料保存后公共详情刷新，新顺序在同页与跨标签页可见。

自动测试使用受控 URL 和可模拟的图片加载事件，不依赖官网、Spotify 或网易云在线状态。相关测试补充简洁“测试点”注释；不以 CSS 类名、像素值或源码关键字断言代替业务和视觉证据。

### 8.3 执行命令与本地浏览器

本次涉及 frontend 与 backend，最终检查：

```powershell
python scripts/run_checks.py functional
```

数据库集成场景保持串行。视觉和实际交互在隔离测试库上验证，沿用现有测试管理员；优先使用独立 Chrome/Edge；本次工具只提供内置浏览器，因此实际使用内置浏览器验证。服务连接 `live_statistic_test`，不启用 Uvicorn reload，结束后停止。

本地浏览器至少检查空数组、单图、多图、首图失败、长 URL、长专辑标题和窄屏布局。可选真实外链冒烟仅验证当时的网络可用性，不把第三方服务可用性设为自动测试成功条件。

### 8.4 本地验证记录（2026-09-27）

- `python scripts/run_checks.py functional`：通过；前端 48 个测试文件、477 项测试通过。
- `live_statistic_test`：Flyway 从 V40 增量升级到 V41，随后 validate 通过。
- 临时空数据库：完整 B1–V41 迁移链及所有权契约通过；测试完成后删除该临时库。
- 集成测试覆盖旧数据迁移守卫、专辑 ID/revision/曲目保持、数组约束、链接顺序、省略与清空、角色、CSRF 和修订冲突。
- 浏览器使用既有测试管理员完成创建、设为默认、确认保存及公开详情读取；检查真实 HTTPS 图片、失效首图、上一张/下一张及末张禁用、重新展开恢复默认。1280px 宽屏与 390px 窄屏、深浅色均有检查，封面区域未造成整页横向溢出。
- URL 预览、单图/空数组、草稿恢复与保存失败等边界由前端行为测试覆盖。外部图片仅作浏览器冒烟，不作为自动测试依赖。
- 实现验收阶段只迁移测试库；后续经用户授权的本地主库迁移见下一节。未执行远程部署。历史 V40 回填包仍仅适用于保留 `cover_path` 的 schema 阶段，未改动其 SQL、模板和哈希。


### 8.5 本地主库迁移与视觉复验（2026-09-27）

- 目标：Docker `live-set-list-docker`，`localhost:15432/live_statistic`。执行前为 V40，唯一待执行项为 V41。
- 先执行 `python scripts/recovery_db.py backup-app-manual`，备份文件 `live_statistic_manual_20260927_022126.dump`；`pg_restore -l` 与恢复 SQL 预览通过（42,032 行）。
- 按 `info → migrate → validate → info` 完成 V40 → V41；所有权契约及 `/api/health/db` 通过。
- 180 张专辑、1,255 条收录、198 个专辑子项按迁移前所有原始列逐行比对一致，包含 ID、修订号和曲目关系。新增字段初始化为 null / []。
- 主库实际歌曲及专辑详情、后台编辑回填均正常。现有专辑尚未补录封面，主库验证没有保存测试 URL。
- 单图、多图、坏图、默认状态恢复在独立测试库复验。1280px 宽屏与 390px 窄屏、浅色与深色页面未发现新增溢出；单图不显示翻页，多图末张禁用下一张，失败图片仍可切回。
- 本次两个测试专辑及关联曲目已清理；临时服务已结束。没有远程数据库或部署操作。
- 截图与完整本地记录保存在 `.tmp/album-cover-visual/`。
