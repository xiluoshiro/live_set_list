# 既有 Live 时区快照盘点（2026-09-12）

## 范围与结论

本盘点最初只读取本地 `live_statistic`；2026-09-13 在用户确认并重新同步本地主库后，按下文批次完成本地回填。没有连接或修改远程环境。范围是 V31 迁移后原本满足 `live_attrs.timezone_source = 'legacy_offset'` 的 594 条 Live；这里的“既有”表示迁移前记录，日期实际覆盖 2015-04-18 至 2027-04-25，并不等同于全部已经演出完毕。

首轮结果：

| 分类 | 数量 | 当前判断 |
|---|---:|---|
| Venue 候选时区与旧偏移一致 | 575 | 已回填，排期钟点和兼容偏移不变 |
| 已确认县级地区、与旧偏移一致 | 2 | 已通过沖縄県行政区记录回填 `Asia/Tokyo` |
| 已确认旧偏移错误 | 1 | 已将上海 Live 377 的三个偏移从 `+09:00` 修为 `+08:00` |
| ONLINE 保持不动 | 16 | 永久保留 `legacy_offset`，不猜测活动时区 |
| 合计 | 594 | 578 条为 `venue` 快照，16 条仍为 `legacy_offset` |

原 576 条记录通过稳定 `venue_id` 关联到已核验 Venue 所在地区，候选时区均来自 `geo_localities.timezone_id`；没有一条使用 Venue 自身的精确 `venue_list.timezone_id`。回填审计明确标记为后续核验，不能把 2026 年完成的所在地核验伪装成 Live 创建时已经保存的历史快照。

## 判定规则

1. 只盘点 `timezone_source = 'legacy_offset'`，不改动已经具有 IANA 快照的新记录。
2. 通过 Live 已绑定的稳定 `venue_id` 读取 `venue_list.timezone_id`，为空时再读取 Venue 的 `geo_localities.timezone_id`。
3. 使用候选 IANA 时区在 `live_date` 当地中午计算该日 UTC 偏移，避免拿当前偏移错误判断历史夏令时。
4. 将计算值与 `timezone_offset_minutes` 比较；现有 594 条记录的 `opening_time`、`start_time` 所带偏移均与该兼容字段一致，没有发现同一行内部的偏移矛盾。
5. `ONLINE` 不参与回填；其他没有候选时区的记录默认归入证据不足。只有像“沖縄県某所”这样经人工确认县级范围足以唯一确定 IANA 时区的例外，才进入独立确认批次。

实体 Venue 的业务规则是实际搬迁应新建 Venue，因此稳定 `venue_id` 加已核验所在地可以作为强候选依据；正式写入前仍需明确记录这是后续核验回填，而不是原始历史字段。

## 已确认的偏移错误

| Live ID | 日期 | Live | Venue | 旧偏移 | 候选时区 | 当日应有偏移 |
|---:|---|---|---|---:|---|---:|
| 377 | 2018-07-22 | BilibiliWorld 2018 DAY3 | 上海世博展览馆 | `+09:00` | `Asia/Shanghai` | `+08:00` |

用户已确认该记录的上海场馆资料正确，错误来自历史时区资料。已保存 `Asia/Shanghai`，将兼容偏移和开场／开演 `timetz` 偏移一并从 `+09:00` 修为 `+08:00`，保持 `live_date` 及 09:00／10:30 墙上钟点不变，并在审计中保留旧、新值。

## 明确保留不动的 ONLINE 16 条

Live ID 为 `496, 497, 498, 499, 502, 503, 504, 506, 510, 511, 514, 515, 519, 520, 531, 533`，日期覆盖 2020-10-18 至 2021-07-10，旧偏移全部为 `+09:00`。

用户已确定这 16 条全部不动。它们继续保留 `timezone_source = 'legacy_offset'`、空 `timezone_id` 及现有 `+09:00`，不再进入活动资料核验或本次回填范围。

## 县级地区足以确认时区的 2 条

| Live ID | 日期 | Live | Venue | 旧偏移 |
|---:|---|---|---|---:|
| 54 | 2026-07-04 | 夢限大みゅーたいぷ 47都道府県制覇の旅「スーパーポジション ～ゆめんそーれ沖縄～」DAY 1 | 沖縄県某所 | `+09:00` |
| 55 | 2026-07-05 | 夢限大みゅーたいぷ 47都道府県制覇の旅「スーパーポジション ～ゆめんそーれ沖縄～」DAY 2 | 沖縄県某所 | `+09:00` |

用户确认只记录到县级，不新增城市。官方活动资料公布地点为“沖縄県”；该县单一使用 `Asia/Tokyo`，因此两条 Live 的时区可以确认。

V32 已允许行政区层级。已新增 `JP / 沖縄県 / admin_area / Asia/Tokyo`（`locality_name=NULL`），Venue 32 关联该地区，`location_revision` 从 1 增至 2；其地址、坐标和 Venue 自身的精确 `timezone_id` 仍为空。Live 54、55 均以该 Venue 修订号保存 `timezone_source='venue'`、`timezone_id='Asia/Tokyo'`，旧 `+09:00` 兼容偏移与墙上钟点不变。没有虚构糸満市或具体场馆坐标。

## 改期历史边界

594 条中有 3 条 Live 存在 `live_schedule_history`，共 4 条历史记录：Live 221 一条、Live 239 两条、Live 389 一条。这些 V31 以前形成的历史行，其 `previous_timezone_source` 和 `previous_timezone_offset_minutes` 都为空。

当前 Live 快照的回填没有顺带补造旧改期历史。4 条历史行继续保持“当时没有保存时区证据”的事实；如果以后确需补齐，需要另一份逐次改期资料盘点和独立审计规则。

## 本地主库执行与验收（2026-09-13）

执行文件：[2026-09-13__backfill_verified_live_timezones.sql](../../backend/db/postgres/backfill/2026-09-13__backfill_verified_live_timezones.sql)。它不是 Flyway 迁移，只允许对本机 `live_statistic` 的 V32 基线执行。SQL 冻结了 576 条实体候选的日期、场馆、开场／开演时间、旧偏移、候选时区及修订号指纹；按 575／2／1 三批更新，任何数量或原值漂移会使整笔事务回滚。16 条 ONLINE 排除在外。该一次性脚本不应在已回填库重复执行；重复执行会在前置断言处停止，不会再次修改数据。

- 手动备份：`live_statistic_manual_20260913_205749.dump`，`pg_restore -l` 和最小恢复 SQL 校验通过（35926 行）。
- 同一 SQL 先以事务 `ROLLBACK` 演练，所有前置、后置断言通过；正式执行以 `COMMIT` 结束。
- 本地主库 V32 不变；新增 1 条沖縄県行政区、关联 Venue 32；575 条普通一致记录和 Live 54／55 保持排期原值，Live 377 修正三个偏移。
- 回填后分类为 `venue=578`、`legacy_offset=16`；578 条 Live、1 条 locality、1 条 Venue 各有审计记录；4 条旧改期历史未改；所有已回填的开场／开演偏移与兼容偏移一致。
- 2026-09-13 `python scripts/run_checks.py functional` 退出码 0：后端单元和集成测试、前端 40 个文件／422 项测试、恢复工具 25 项测试全部通过。测试库 seed 独立于本地主库，主库数据结果以本节只读 SQL 验收为准。

后续目标不再是重复回填这 594 条，而是确认实际部署环境的数据同步／独立执行状态，并评估未公布地区的通用编辑流程及未来 Live 的资料修订提示。本轮没有获得远程执行授权，也没有远程验证。
